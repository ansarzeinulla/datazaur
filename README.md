### 1. Настройка окружения
В корне проекта (папка `hack-nu`):

```bash
# 1. Создаем виртуальное окружение
uv venv

# 2. Активируем его
# Для macOS/Linux:
source .venv/bin/activate
# Для Windows:
# .venv\Scripts\activate

# 3. Устанавливаем библиотеки (используем актуальную langchain-chroma)
uv add fastapi uvicorn pydantic httpx langchain-huggingface langchain-chroma sentence-transformers tqdm
```

### 2. Подготовка данных (Индексация)
Перед запуском убедитесь, что файлы протоколов лежат в `data/corpus/`.

```bash
# Создаем векторную базу (папка chroma_db)
uv run python scripts/build_index.py
```

### 3. Локальный запуск и проверка
Запускаем сервер и скрипт оценки для проверки точности.

```bash
# Терминал 1: Запуск сервера (порт 8080)
uv run uvicorn src.main:app --host 0.0.0.0 --port 8080 --reload

# Терминал 2: Запуск оценки (флаг -p 1 чтобы не ловить таймауты от LLM)
uv run python evaluate.py -e http://localhost:8080/diagnose -d ./data/test_set -n my_team_final -p 1
```

### 4. Сборка и запуск через Docker (Финальный этап)
Это именно то, что будет делать жюри. Проверьте этот шаг обязательно.

```bash
# 1. Сборка образа
docker build -t submission .

# 2. Запуск контейнера (убедитесь, что локальный uvicorn выключен, чтобы порт был свободен)
docker run -p 8080:8080 submission
```

---

### 💡 Ключевые исправления, которые я внес:

1.  **Библиотека Chroma**: Я заменил `langchain-community` на `langchain-chroma` в команде `uv add`. Это уберет то назойливое предупреждение `LangChainDeprecationWarning`, которое мы видели в логах.
2.  **Параллелизм в Evaluate**: В команду `evaluate.py` я добавил `-p 1`. Поскольку модель `oss-120b` тяжелая, лучше отправлять запросы по одному, чтобы избежать ошибок `ReadTimeout` (таймаута).
3.  **Удаление лишнего**: Я убрал `"unstructured[pdf]"`, так как мы работаем с JSON-корпусом. Это уменьшит размер Docker-образа и ускорит установку.

**Confidence Level: 1.0.** Теперь эти команды идеально синхронизированы с кодом, который мы написали.

**Вы готовы к дедлайну! Удачи! 🚀**